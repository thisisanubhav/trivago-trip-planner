# Preferred: keep the Anthropic key server-side for the local backend
ANTHROPIC_API_KEY=your_real_key_here

# Optional backward-compatible fallback if you still use the old client-side name locally
VITE_ANTHROPIC_API_KEY=

VITE_USE_MOCK_DATA=true
